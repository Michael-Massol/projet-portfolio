{% extends 'base.html.twig' %}

{% block title %}Interet{% endblock %}

{% block body %}
    <h1>Interet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interet.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ interet.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ interet.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interet_index') }}">back to list</a>

    <a href="{{ path('app_interet_edit', {'id': interet.id}) }}">edit</a>

    {{ include('interet/_delete_form.html.twig') }}
{% endblock %}
