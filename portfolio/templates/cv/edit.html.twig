{% extends 'base.html.twig' %}

{% block title %}Edit CV{% endblock %}

{% block body %}
    <h1>Edit CV</h1>

    {{ include('cv/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_c_v_index') }}">back to list</a>

    {{ include('cv/_delete_form.html.twig') }}
{% endblock %}
